{
  "name": "skycoin4444-analytics-core",
  "version": "1.0.0-beta.1",
  "description": "Bounded deterministic event aggregation primitives for SKYCOIN4444 integrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "pnpm run build && node tests/analytics.test.js",
    "audit": "pnpm audit --audit-level=high",
    "prepack": "pnpm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.15.1",
  "keywords": [
    "skycoin4444",
    "analytics",
    "aggregation",
    "events",
    "metrics"
  ],
  "author": "SKYCOIN4444",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
